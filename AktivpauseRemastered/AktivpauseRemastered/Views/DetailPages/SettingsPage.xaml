<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="AktivpauseRemastered.Views.SettingsPage"
             Title="Settings (hc)">
    
    <ContentPage.Content>
        
        <StackLayout VerticalOptions="Fill">
            
            <Label Text="Name: (hc)"/>
            <Entry x:Name="NameEntry"
                   Text="old val(hc)"/>
            <Label Text="Language: (hc)"/>
            <Picker x:Name="LanguagePicker"
                    Title="Pick a language (hc)">
            </Picker>
            <Label Text="Step Length: (hc)"/>
            <Entry x:Name="StepLengthEntry"
                   Keyboard="Numeric"
                   />

            <Label Text="Sample Rate: (hc)"/>
            <Picker x:Name="SampleRatePicker"
                    Title="Pick a Samplerate (hc)"
                    ItemsSource="{Binding SampleRateItemsSource}"
                    ItemDisplayBinding = "{Binding .}"/>
            <Button x:Name="SaveButton"
                        Text="Save(hc)"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>